APP_ENV=test
APP_DEBUG=0
APP_SECRET=test_app_secret_phpunit

# SQLite in-memory : base de données fraîche à chaque démarrage du kernel
DATABASE_URL="sqlite:///:memory:"

# Clé JWT dédiée aux tests (courte suffit)
JWT_SECRET=phpunit_jwt_secret_test_key
